# Local Development Environment Variables for Auth Service
PORT=8001
NODE_ENV=development

# Database Configuration (Local Docker)
DB_HOST=localhost
DB_PORT=5434
DB_NAME=dr_assistant
DB_USER=postgres
DB_PASSWORD=postgres

# Redis Configuration (Local Docker)
REDIS_HOST=localhost
REDIS_PORT=6380

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_for_local_dev
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=debug

# CORS
CORS_ORIGIN=http://localhost:3000
